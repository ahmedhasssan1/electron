// Re-exports from barrel — kept for backward compatibility.
// Prefer importing from 'dto/user' directly.
export {
  createUserSchema,
  updateUserSchema,
  type CreateUserDTO,
  type UpdateUserDTO,
} from './user';
